{
  "name": "greenwood-school-website",
  "version": "1.0.0",
  "private": true,
  "description": "Greenwood International School website",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "start": "npm run dev --prefix client",
    "build": "npm run build --prefix client"
  }
}
